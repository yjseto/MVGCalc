import math
from enum import Enum

class Constants(Enum):
    PI          = (1,"math.pi")
    EULERSNUM   = (2,"math.e")
    TAU         = (3,"math.tau")
    INFINITY    = (4,"math.inf")
    NAN         = (5,"math.nan")